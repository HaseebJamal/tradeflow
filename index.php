<?php

// Use this front controller only when the host must serve the project one
// level above Laravel's public web root. Laravel's actual public entry point
// remains public/index.php; loading it from here keeps /public out of URLs.
require __DIR__.'/public/index.php';
