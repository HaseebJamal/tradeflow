@extends('layouts.dashboard')
@section('page-title', isset($category) ? 'Edit Category' : 'Add Category')
@section('page-subtitle', 'Create a product category for this business')
@section('content')
<div class="tf-card p-4" style="max-width:720px"><form method="POST" action="{{ isset($category) ? route('business.categories.update', $category) : route('business.categories.store') }}" class="row g-3">@csrf @isset($category)@method('PUT')@endisset <div class="col-md-8"><label class="form-label">Category Name <span class="text-danger">*</span></label><input name="name" class="form-control @error('name') is-invalid @enderror" value="{{ old('name', $category->name ?? '') }}" required autofocus>@error('name')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-4"><label class="form-label">Status <span class="text-danger">*</span></label><select name="status" class="form-select"><option value="Active" @selected(old('status', $category->status ?? 'Active') === 'Active')>Active</option><option value="Inactive" @selected(old('status', $category->status ?? '') === 'Inactive')>Inactive</option></select></div><div class="col-12"><label class="form-label">Description <span class="text-muted">(Optional)</span></label><textarea name="description" rows="3" class="form-control">{{ old('description', $category->description ?? '') }}</textarea></div><div class="col-12 d-flex gap-2"><button class="btn btn-tf-primary">{{ isset($category) ? 'Update Category' : 'Save Category' }}</button><a class="btn btn-outline-secondary" href="{{ route('business.categories.index') }}">Cancel</a></div></form></div>
@endsection
